/**
 * @brief  Small struct for storing the coordinates of a particular value.
 * @note   the value itself is not stored here. 
 * @retval None
 */
typedef struct Tuple
{
    int row;
    int column;
} Tuple;